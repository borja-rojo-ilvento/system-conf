# TODO

Open items, scoped to this repo.

- [ ] Security audit
- [ ] Use `sops-nix` or `agenix` to store secrets in the repo. Things like ssh public-private key
pairs, wifi passwords, things like that.
- [ ] Setup `cloudflared` and Cloudflare tunnel to my server to open different things up to the
internet.
